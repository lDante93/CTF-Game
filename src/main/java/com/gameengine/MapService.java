package com.gameengine;

import com.domain.Flag;
import com.domain.Player;

public class MapService {
//
////
////    public static Character[][] initializeAndfillArray(int rows,int columns) {
////        MapService.rows = rows;
////        MapService.columns = columns;
////        map = new Character[MapService.rows][MapService.columns];
////
////        for (int i = 0; i < map.length; i++) {
////
////            for (int j = 0; j < map[0].length; j++) {
////                map[i][j] = ' ';
////            }
////
////        }
////        return map;
////    }
////
////    public static Character[][] restartMap() {
////        map = new Character[MapService.rows][MapService.columns];
////
////        for (int i = 0; i < map.length; i++) {
////
////            for (int j = 0; j < map[0].length; j++) {
////                map[i][j] = ' ';
////            }
////
////        }
////        return map;
////    }
//
//
//    public static void setMyPlayerLocation(Player player) {
//        changeElement(player.getX(), player.getY(), 'P');
//    }
//
//
//    public static void setOpponentPlayerLocation(Player opponent) {
//        changeElement(opponent.getX(), opponent.getX(), 'O');
//    }
//
//    public static void setFlagLocation(Flag flag) {
//        changeElement(flag.getX(), flag.getY(), 'F');
//    }
//
////    public static void setWallLocation(Wall wall) {
////        changeElement(row, column, 'X');
////    }
////
////    public static void setWaterLocation(Integer row, Integer column) {
////        changeElement(row, column, 'W');
////    }
////
////    public static void setSwampLocation(Integer row, Integer column) {
////        changeElement(row, column, 'S');
////    }
//
//    public static void changeElement(Integer row, Integer column, Character element) {
//        if ((row >= 0 && row <= 30) && (column >= 0 && column <= 30)) {
//            map[column - 1][row - 1] = element;
//        } else {
//            System.err.println("You want to change out of array element.\nYou want to change element at rowNumber: " + row + " and columnNumber: " + column + ".");
//        }
//    }

}
